# AGENTS.md

这个仓库用于记录模型部署暑假学习过程，不是正式简历项目仓库。

## 学习目标

- 两个月内建立模型部署、边缘 AI、Jetson 视觉部署能力。
- 暑假结束后能开始投递实习和秋招相关岗位。
- 至少完成两个就业相关项目：
  - Severstal 钢材缺陷语义分割 + ONNX/TensorRT + Triton/FastAPI GPU 推理服务项目。
  - Jetson Orin Nano C++ 实时多目标跟踪与安全事件分析系统，可扩展为视觉小车。
- 已记录项目 3 候选方向：Jetson + STM32 视觉安全控制原型，使用 FreeRTOS、
  SocketCAN/CAN、PWM、编码器和失联安全状态机，把项目 2 的版本化风险事件
  转换为物理减速与停车。项目 3 不复制项目 2 的视觉流水线；是否启动及何时
  启动由用户明确决定。
- 已完成 Kaggle Digit Recognizer 有效提交和一次有记录优化；该记录作为历史保留。
  2026-08-28 起不再严格按 Day 顺序执行，后续任务由用户的当次明确需求驱动。

## 当前执行方式（2026-08-28 起）

- 用户当次明确提出的需求拥有最高优先级，可以跳过、替换或终止路线中的候选项。
- `roadmap/60-day-plan.md` 保留历史进度和候选任务，不再是必须逐日顺序执行的队列。
- 恢复上下文后不得自动推断“下一天”，也不得仅因旧路线包含某项优化就主动开始。
- 只有用户明确说“开始 DayXX”或要求当天学习计划时，才按 Day 形式给出 7 小时计划并维护 `logs/dayXX.md`。
- 普通开发、分析、采购或文档需求按任务本身推进；更新相关主题文档，不虚构 Day 编号或学习日志。
- 重要改动仍需验证、记录、提交并推送；正式项目仓库边界和禁止元话语规则保持不变。
- 竞赛不自动重新插入；如用户未来明确提出新的竞赛需求，再单独评估。

## 用户背景

- 大三，电子信息工程。
- 每天可学习约 7 小时。
- Python 入门，C++ 低于入门但有 C 语言基础。
- Linux 基本没有基础。
- 深度学习入门。
- 有 GitHub 账号：Nefelibata134。
- 已购买 Jetson Orin Nano 8GB，配有 256GB NVMe SSD，并已完成首次初始化、JetPack/TensorRT 检查和 SSH/串口连接。
- 已购买 IMX219 CSI 摄像头，作为项目 2 的主要实时输入；文件回放仍作为可复现基线，RTSP 作为网络输入与恢复测试。
- 不考研，暑假结束后准备秋招和实习。

## 上下文重置后的恢复流程

`AGENTS.md` 是本协作的长期记忆入口。每次上下文重置、开启新任务或不确定当前进度时，必须先恢复仓库状态，不能只凭对话残留猜测。

按顺序检查：

1. 阅读 `AGENTS.md`，恢复硬性规则、用户背景和仓库边界。
2. 阅读 `README.md` 的学习进度索引，确认最后一个已完成 Day。
3. 阅读 `roadmap/60-day-plan.md`，恢复历史进度和尚未执行的候选任务，但不把它当作自动执行队列。
4. 阅读最新的 `logs/dayXX.md`，确认当天已完成内容、错误和下一步。
5. 阅读 `projects/README.md` 与 `career/direction-and-jobs.md`，恢复两个项目和就业方向。
6. 执行 `git status --short` 和 `git log -3 --oneline`，确认是否存在未提交改动以及最近完成的工作。

恢复后先用仓库事实确认已完成内容和当前工作树，再等待或执行用户的明确需求。
聊天中的最新明确要求优先于旧路线；不能自行把候选 Day 当成下一任务。路线和日志用于
提供背景、发现依赖与避免重复，不用于覆盖用户对范围和优先级的决定。

## 两个正式项目仓库

两个项目必须分别建立独立 GitHub 仓库，不能把正式核心代码直接堆在本学习仓库中：

| 项目 | 独立仓库名 | 建仓节点 | v1 节点 |
| --- | --- | --- | --- |
| 工业钢材缺陷分割与 GPU 推理服务 | `industrial-defect-inference-service` | Day18 | Day25 |
| Jetson 实时跟踪与安全事件分析系统 | `jetson-realtime-tracking-system` | Day28 | Day40 |

仓库归属统一使用 GitHub 用户 `Nefelibata134`。项目开始时再创建对应仓库；创建前先确认名称尚未占用。

项目 3 的计划仓库名为
`jetson-stm32-vision-safety-controller`。正式启动前只在本学习仓库保存规划和
采购清单；启动后同样使用独立公开仓库。其 Jetson 网关只消费项目 2 的稳定
风险/真实帧健康协议，STM32 侧负责 FreeRTOS、CAN、控制反馈和失效安全，
两个项目之间不得复制 YOLOX、TensorRT、ByteTrack 或视频输入代码。

本学习仓库只保存项目 proposal、学习日志、实验摘要、踩坑记录和项目索引。正式项目仓库保存代码、配置、测试、依赖、模型说明、性能报告、截图位置和项目 README。

项目 2 的 v1 核心技术栈固定为 C++17、CMake、GStreamer、TensorRT C++ API 和 ByteTrack，必须覆盖持续视频流、事件规则、设备监控和故障恢复。DeepStream 仅在 JetPack 兼容性验证通过后作为对照实验，不得阻塞 v1。

两个公开项目仓库是面向招聘者的工程作品，不得出现 `DayXX`、学习任务、教程式打卡或“正在学习”等叙述。开发过程使用 release milestone、issue、ADR、`CHANGELOG.md`、benchmark report 和工程化 commit 表达；学习过程只记录在本仓库。

正式项目仓库也不得出现“招聘展示”“简历项目”“作品集”“给面试官看”
等自我说明用途的元话语。仓库只描述系统本身的问题、架构、实现、接口、
测试、指标、限制和部署方法。`resume` 表示恢复训练、`learning_rate`
表示学习率等正式技术术语不属于上述禁用叙述。

## 协作方式

- 用户明确开始某个 Day 或要求学习日计划时，先给出当天 7 小时学习计划。
- 核心模块必须按“会运行、能解释、能修改”三层推进，不能只让用户运行由 AI 完成的黑盒代码。
- 每个核心模块进入下一阶段前，必须完成：
  - 结合真实输入输出的代码讲解。
  - 与实习、秋招相关的常见面试追问。
  - 至少一个由用户亲自完成的小修改或验证任务。
- 模块提问用于检查理解，不用于机械考试。问题必须紧扣当前项目的真实代码、数据流和设计决策；如果用户暂时答不上，先解释相关代码位置、输入输出、设计原因和失败场景，再让用户用自己的话复述或完成一次小验证，确认理解后才能继续。
- 每完成一个阶段，先说明它解决了项目中的什么问题、与前后模块如何连接，以及用户需要达到“会运行、能解释、能修改”中的哪一层，避免只堆命令和功能。
- 不要求用户机械背诵全部代码，但要求能说明设计原因、关键数据流、验证方法和常见失败场景。
- 路线中未完成的项目增强与就业交付作为候选任务，由用户决定是否执行及执行顺序。
- 只有以 Day 形式启动的学习日，结束后才整理对应的 `logs/dayXX.md`；其他任务更新最相关的主题文档。
- 每次新增学习记录后，更新 `README.md` 的学习进度索引。
- 学习记录、路线、复盘和准备材料放在本仓库。
- 正式项目代码后续单独开仓库，不混入学习计划和打卡日志。
- 不要删除已有学习记录、路线规划、项目规划文件，除非用户明确要求。
- 每次提交前总结改动，并建议清晰的 git commit message。
- 每次重要更新后提交 Git，并推送到 GitHub。

## 仓库定位

本仓库适合放：

- 学习路线。
- 每日学习日志。
- 周复盘。
- 环境搭建记录。
- 采购清单。
- 面试和岗位准备。
- 竞赛准备计划。
- 排错记录。

本仓库不适合放：

- 正式项目的大量核心代码。
- 和模型部署无关的临时文件。
- 简历项目最终展示材料的全部内容。

## 推荐方向

主方向：边缘视觉部署。

理由：

- 和电子信息工程、Jetson、视觉小车最匹配。
- 能覆盖模型部署、TensorRT、ONNX、OpenCV、Linux、摄像头、边缘设备等就业关键词。
- 两个月内更容易做出能展示的作品。

## 日常记录要求

每日日志建议包含：

- 今日目标。
- 学习内容。
- 动手记录。
- 遇到的问题。
- 今日产出。
- 明日计划。
- 复盘。

记录要求：

- 以 Day 形式启动的学习日结束后，必须更新对应的 `logs/dayXX.md`。
- 每次新增或修改当天学习记录后，必须同步更新 `README.md` 的学习进度索引。
- 所有建议都要考虑用户背景：电子信息工程大三，Linux/C++ 较弱，目标是暑假项目积累、实习和秋招。
- 代码项目优先保持可运行、可复现；项目 README 需要以结果和架构为先，写清环境、命令、指标、结果截图与演示位置。

## 文件约定

- 历史进度与候选任务表：`roadmap/60-day-plan.md`
- 个人目标与原则：`roadmap/personal-60-day-plan.md`
- 每日记录：`logs/dayXX.md`
- 项目规划：`projects/README.md`
- 历史竞赛计划：`career/competition-plan.md`（不属于当前 60 天执行表）
- 历史竞赛候选：`career/competition-shortlist-2026-summer.md`（不属于当前 60 天执行表）
- 岗位建议：`career/direction-and-jobs.md`
- Jetson 采购：`hardware/jetson-buying-list.md`
- GitHub 规范：`github-workflow.md`

## 提交前检查

提交前先确认：

- 如果本次任务以 Day 形式启动，是否更新了对应的 `logs/dayXX.md`。
- 如果新增了日志，是否更新了 `README.md` 的学习进度索引。
- 是否误删或大幅改动了既有路线、记录或项目规划。
- 是否能用一句清晰的 commit message 说明本次改动。
